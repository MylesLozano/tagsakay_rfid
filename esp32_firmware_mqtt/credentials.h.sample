/*
 * TagSakay RFID Reader - Credentials File
 * 
 * This file contains all the sensitive credentials and configuration
 * that should not be committed to version control.
 * 
 * Instructions:
 * 1. Rename this file from credentials.h.sample to credentials.h
 * 2. Fill in your actual credentials and settings
 * 3. Do NOT commit credentials.h to version control
 */

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// WiFi Credentials
const char* WIFI_SSID = "your-wifi-ssid";
const char* WIFI_PASSWORD = "your-wifi-password";

// MQTT Credentials
const char* MQTT_SERVER = "your-mqtt-server.com";
const int MQTT_PORT = 1883;
const char* MQTT_USERNAME = "your-mqtt-username";
const char* MQTT_PASSWORD = "your-mqtt-password";

// API Authentication (for HTTP communication)
const char* API_KEY = "your-api-key";
const char* API_URL = "https://your-tagsakay-api.com/api/rfid/scan";

// Default Device Location
// This can be changed via the web interface
#define DEFAULT_DEVICE_LOCATION "Bus Terminal"

#endif /* CREDENTIALS_H */
